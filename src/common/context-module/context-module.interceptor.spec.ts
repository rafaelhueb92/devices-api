import { ContextModuleInterceptor } from './context-module.interceptor';

describe('ContextModuleInterceptor', () => {
  it('should be defined', () => {
    expect(new ContextModuleInterceptor()).toBeDefined();
  });
});
